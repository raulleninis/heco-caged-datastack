{
  "name": "caged-arquivo",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "build": "esbuild src/login.js --bundle --format=esm --minify --outfile=public/assets/login.js",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "dependencies": {
    "@netlify/edge-functions": "4.0.0",
    "@netlify/identity": "2.0.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
