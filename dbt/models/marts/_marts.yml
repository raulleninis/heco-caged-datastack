version: 2

models:
  - name: mart_caged_mensal_grupamento
    description: "Admissões, desligamentos e saldo líquido do CAGED por competência e grupamento, materializada no Postgres."
    columns:
      - name: competencia_mov
        tests:
          - not_null
      - name: grupamento
        tests:
          - not_null
      - name: saldo_liquido
        tests:
          - not_null
