name: 'caged_analytics'
version: '1.0.0'
config-version: 2

require-dbt-version: ">=1.10.0"

profile: 'caged_analytics'

model-paths: ["models"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  caged_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
